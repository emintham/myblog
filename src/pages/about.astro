---
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "About"; // This will be styled like "Latest Writings"
const pageIntroText =
  "A little more about this space, its purpose, and the voice behind it."; // A brief intro text below the title
---

<BaseLayout title={pageTitle} description={pageIntroText}>
  <section class="about-page-container">
    <h1 class="section-title">{pageTitle}</h1>

    <p class="page-intro-text">{pageIntroText}</p>

    <div class="prose-content-area">
      {/* For the main body of the about page */}
      <p>
        This is a space dedicated to [mention your blog's primary themes, e.g.,
        thoughtful exploration, simple living, technological insights, creative
        pursuits]. Inspired by clarity and a desire for focused reading, the
        design here is intentionally spare, allowing words and ideas to take
        precedence.
      </p>
      <p>
        I believe in [mention a core philosophy or interest that drives the
        blog, e.g., the beauty of well-crafted things, the power of quiet
        observation, the importance of continuous learning]. This blog serves as
        a personal journal and a place to share reflections, discoveries, and
        perhaps, to spark a quiet conversation.
      </p>
      <p>Thank you for spending a moment here.</p>

      {
        /* Optional:
      <h2 class="subsection-heading-style">Contact</h2>
      <p>
        If you'd like to get in touch, you can reach out via [mention email if comfortable, or a professional social media link].
      </p>
      */
      }
    </div>
  </section>
</BaseLayout>

<style>
  /* Container for the main prose of the about page */
  .prose-content-area {
    /* Typography (font-family, font-size, line-height, color)
       will largely be inherited from 'body' styles in global.css */
  }

  .prose-content-area p {
    /* These will also inherit from global 'p' styles. */
    max-width: 65ch; /* Optimal line length for readability */
    margin-left: auto; /* Center the prose block if narrower than content-wrapper */
    margin-right: auto; /* Center the prose block */
    margin-bottom: 1.5em;
  }

  /* Optional styling for H2 if you add subsections */
  .subsection-heading-style {
    /* This is an H2 */
    font-family: var(--font-sans);
    font-size: 1.8rem;
    color: var(--color-text);
    max-width: 65ch; /* Align with prose */
    margin: 3em auto 1em auto; /* Centering and vertical spacing */
    padding-bottom: 0.5em;
    /* border-bottom: 1px solid #f0f0f0; /* Lighter border for subsections - optional */
  }
</style>
