# Blog Template Environment Variables

# Site Configuration
# Override defaults in src/siteConfig.ts by uncommenting and setting these
SITE_TITLE=My Blog
SITE_DESCRIPTION=A minimalist blog
AUTHOR_NAME=Your Name
PUBLIC_SITE_URL=https://yourdomain.com

# Remark42 Comments (Optional)
# Uncomment and configure if using Remark42 for comments
# REMARK42_HOST=https://remark42.yourdomain.com
# REMARK42_SITE_ID=your-site-id

# RAG Configuration (Optional)
# For non-secret configuration, edit src/ragConfig.ts directly
# Environment variables override the defaults in ragConfig.ts

# Force a specific embedding provider instead of auto-detection
# Options: 'auto' | 'ollama' | 'transformers'
# Default: auto (prefers Ollama if available, falls back to transformers.js)
# Note: Requires dev server restart to take effect
# RAG_EMBEDDING_PROVIDER=auto

# Ollama configuration
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# AI Writing Assistant configuration
# Default model for AI assistant (if not selected in UI)
# OLLAMA_MODEL=llama3.2

# Transformers.js configuration
# TRANSFORMERS_EMBEDDING_MODEL=Xenova/all-MiniLM-L6-v2

# Custom RAG data directory
# Default: ./data/rag
# RAG_DATA_DIR=./data/rag
